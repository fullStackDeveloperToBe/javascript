const array = [1,2,3,4,5,6];

function double(x) {
    return 2*x;
}

function triple(x) {
    return 3*x;
}

function binary(x) {
    return x.toString(2);
}
const mapValue = array.map((binary));

console.log(mapValue);